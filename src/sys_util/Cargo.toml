[package]
name = "sys_util"
version = "0.1.0"
authors = ["The Chromium OS Authors"]
build = "build.rs"

[dependencies]
data_model = { path = "../data_model" }
libc = "*"
syscall_defines = { path = "../syscall_defines" }
lazy-init = "0.5.0"

[build-dependencies]
gcc = "=0.3.54"
cc = "1.0"
